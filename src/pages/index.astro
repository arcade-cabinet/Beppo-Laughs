---
import Home from '../../client/src/pages/Home';
import Layout from '../layouts/Layout.astro';
---

<Layout title="Beppo Laughs - Horror Maze Game">
  <!-- 
    client:only="react" ensures this runs ONLY on client side
    This is critical for Three.js/WebGL which needs browser APIs
  -->
  <Home client:only="react" />
</Layout>
