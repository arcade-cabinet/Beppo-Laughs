# Beppo Laughs - Repository-Specific Settings
# ══════════════════════════════════════════════════════════════════════════════
# This file configures repository settings via https://github.com/apps/settings
# Game repos have unique needs: release gates, E2E testing, deployment environments
# ══════════════════════════════════════════════════════════════════════════════

# Inherit org defaults, override what we need
_extends: .github

# ─────────────────────────────────────────────────────────────────────────────
# Repository Settings
# ─────────────────────────────────────────────────────────────────────────────
repository:
  description: "🤡 Beppo Laughs - A horror clown maze game"
  topics: game, horror, maze, typescript, react, threejs, arcade-cabinet
  has_wiki: false
  has_projects: true
  allow_auto_merge: true
  delete_branch_on_merge: true

# ─────────────────────────────────────────────────────────────────────────────
# Repository Rulesets - Game-Specific Rules
# ─────────────────────────────────────────────────────────────────────────────
rulesets:
  # ═══════════════════════════════════════════════════════════════════════════
  # Main Branch Protection
  # ═══════════════════════════════════════════════════════════════════════════
  - name: Main Branch Protection
    target: branch
    enforcement: active
    conditions:
      ref_name:
        include:
          - "~DEFAULT_BRANCH"
        exclude: []
    rules:
      # Prevent force pushes and deletions
      - type: deletion
      - type: non_fast_forward

      # Require linear history
      - type: required_linear_history

      # Require PRs
      - type: pull_request
        parameters:
          required_approving_review_count: 0
          dismiss_stale_reviews_on_push: false
          require_code_owner_review: false
          require_last_push_approval: false
          required_review_thread_resolution: true

      # Enable merge queue for CI efficiency
      - type: merge_queue
        parameters:
          check_response_timeout_minutes: 60
          grouping_strategy: ALLGREEN
          max_entries_to_build: 5
          max_entries_to_merge: 5
          merge_method: SQUASH
          min_entries_to_merge: 1
          min_entries_to_merge_wait_minutes: 5

    bypass_actors:
      - actor_id: 5
        actor_type: RepositoryRole
        bypass_mode: always

# ─────────────────────────────────────────────────────────────────────────────
# Environments - Game-Specific Deployment Environments
# ─────────────────────────────────────────────────────────────────────────────
environments:
  # GitHub Pages - Static site deployment target
  - name: github-pages
    wait_timer: 0
    reviewers: []
    deployment_branch_policy:
      protected_branches: true

# ─────────────────────────────────────────────────────────────────────────────
# Labels - Game-Specific Labels
# ─────────────────────────────────────────────────────────────────────────────
labels:
  # Game development labels
  - name: game/physics
    color: "1d76db"
    description: "Physics system changes"
  - name: game/rendering
    color: "1d76db"
    description: "Rendering/graphics changes"
  - name: game/audio
    color: "1d76db"
    description: "Audio system changes"
  - name: game/ui
    color: "1d76db"
    description: "User interface changes"
  - name: game/gameplay
    color: "1d76db"
    description: "Gameplay mechanics"
  - name: game/maze
    color: "1d76db"
    description: "Maze generation and navigation"
  - name: game/horror
    color: "8b0000"
    description: "Horror elements and atmosphere"

  # CI/CD labels
  - name: ci/cd
    color: "0e8a16"
    description: "CI/CD pipeline changes"
  - name: dependencies
    color: "0366d6"
    description: "Dependency updates"
  - name: ready-for-merge-queue
    color: "6f42c1"
    description: "Ready to enter merge queue"
